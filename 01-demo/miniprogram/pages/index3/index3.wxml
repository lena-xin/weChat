<view>
  hello world || {{msg}}
  <view>num:{{num}}</view>
  <view>isBoy:{{isBoy}} </view>
  <view>name:{{person.name}},age:{{person.age}},score:{{person.score}}</view>
  <view data-id="{{title}}">{{title}}</view>
</view>
